```bash
pacman -Syuu         # update
pacman -S <appname>  # アプリのインストール
pacman -Ss <appname> # アプリの検索
pacman -Rs <appname> # 依存ファイルも含めて削除
pacman -Qe           # 任意で入れたアプリの一覧を表示
pacman -Qs <appname> # 任意で入れたアプリを検索
```
